import { createMuiTheme } from "@mui/material";

const appTheme = createMuiTheme({
  typography: {
    button: {
      textTransform: "none",
    },
    fontSize: 14,
  },
});

export default appTheme;
