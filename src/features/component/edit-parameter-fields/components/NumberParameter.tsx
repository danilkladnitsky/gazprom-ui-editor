import { NumberProperty } from 'entities/properties'
import React from 'react'

type Props = {
    property: NumberProperty;
}

export const NumberParameter = ({ property }: Props) => {
  return (
    <div>NumberParameter</div>
  )
}
