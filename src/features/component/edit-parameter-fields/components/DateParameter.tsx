import { DateProperty } from 'entities/properties'
import React from 'react'

type Props = {
    property: DateProperty;
}

export const DateParameter = ({ property }: Props) => {
  return (
    <div>DateParameter</div>
  )
}
